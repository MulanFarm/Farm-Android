<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/activityBg</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- <item name="android:textCursorDrawable">@drawable/edit_black_cursor</item>-->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:textViewStyle">@style/textStype</item>
        <item name="android:editTextStyle">@style/editStype</item>
    </style>

    <style name="textStype">
        <item name="android:textColor">#333</item>
    </style>

    <style name="editStype">
        <item name="android:background">@null</item>
    </style>
</resources>
